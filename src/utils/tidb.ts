export async function submitIntro(..._args: unknown[]): Promise<void> {
  // Intro submissions are a P-Stream community feature. The copied UI keeps the
  // integration point, but Cineflix does not submit to TheIntroDB yet.
}
